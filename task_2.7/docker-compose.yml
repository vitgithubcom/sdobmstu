version: '3.8'

services:
  auth-service:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DB_PATH=/app/users.db
    volumes:
      - ./data:/app/data
      - ./users.db:/app/users.db
    restart: unless-stopped